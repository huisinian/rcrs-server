问题 1：Agent.java:184 的 try-catch 为什么要包裹 think ()？如果不 catch 会怎样？
为什么要包裹？
think () 方法在执行过程中，很可能会抛出异常，比如受检的 Exception 或者运行时异常。用 try-catch 包裹的核心目的有几个：
捕获异常，防止异常直接抛给 JVM 导致整个程序崩溃；
通过 e.printStackTrace () 打印异常栈信息，方便开发人员快速定位问题根源；
让程序在处理完异常后，还能继续执行后续逻辑，而不是直接终止。
如果不 catch 会怎样？
如果不使用 try-catch 捕获，异常会沿着调用链向上抛出。如果调用链上也没有其他处理逻辑，异常最终会抛给 JVM，直接导致程序终止运行，后续代码无法执行，同时也不会保留详细的异常栈信息，给线上问题排查带来很大困难。
问题 2：项目中为什么各处都打日志？日志的 4 个级别分别对应什么场景？
项目中为什么要打日志？
日志在项目里的作用非常关键，主要有这几点：
问题排查：线上环境无法直接调试，日志是定位问题的核心依据，能帮你还原问题发生时的上下文；
流程追踪：记录关键业务流程的执行情况，比如配置加载、请求处理、数据流转等；
监控告警：ERROR 和 WARN 级别的日志可以对接监控系统，实现异常告警，提前发现潜在问题；
审计追溯：记录关键操作，方便后续对业务行为进行审计和追溯。
日志的 4 个级别分别对应什么场景？
DEBUG 级别主要用于开发和测试环境，记录详细的调试信息，比如变量值、方法调用流程，生产环境通常会关闭这个级别，避免日志量过大。
INFO 级别用于记录正常的业务流程信息，比如 “用户登录成功”“配置文件加载完成”，是生产环境默认开启的级别，用来记录系统正常运行的关键节点。
WARN 级别用于记录潜在的问题，这些问题暂时不会影响核心功能，但可能存在风险，比如 “配置项缺失，使用默认值”“连接池连接数即将耗尽”，需要开发人员关注。
ERROR 级别用于记录影响功能运行的错误事件，比如 “数据库连接失败”“文件读取异常”，这类日志需要重点关注，通常会触发告警通知开发人员处理。